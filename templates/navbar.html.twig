{% block navbar %}

<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->

    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <p class="nav-link">PatchTracking</p>
        </li>
    </ul>
    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">

        <!-- Notifications Dropdown Menu -->
        <li class="nav-item dropdown">
          <a class="nav-link" data-toggle="dropdown" href="#">
            <i class="fa fa-lg fa-user-circle mr-1"></i>
            
            {% if app.user.extraFields is defined %}
              {{ app.user.extraFields.displayName }}
            {% else %}
              {{ app.user.getFullName }}
            {% endif %}
          </a>
          <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
            <a href="{{ path('user.profile') }}" class="dropdown-item">
              <i class="fas fa-user mr-2"></i> Profile
            </a>

            <div class="dropdown-divider"></div>
            <a href="{{ path('security.logout') }}" class="dropdown-item dropdown-footer">Logout</a>
          </div>
        </li>

      </ul>

</nav>
<!-- /.navbar -->
{% endblock %}
